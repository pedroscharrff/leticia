# PLACEHOLDER — substitua antes de subir em produção!
#
# Gere uma linha real com:
#   htpasswd -B -c nginx/htpasswd admin
# (a flag -c CRIA o arquivo, removendo este placeholder)
#
# Em prod o deploy.sh gera automaticamente. Localmente este arquivo precisa
# existir SENÃO o docker compose mount cria um diretório no lugar e o nginx
# falha no boot. O bloco `auth_basic` em metrics.conf bloqueia qualquer
# request enquanto o arquivo só tiver comentários (nenhum hash válido).
